// Stub for useUser - no authentication in UI library mode

export function useUserStable() {
  return {
    user: null,
    loading: false,
  };
}

export function useUser() {
  return {
    user: null,
    loading: false,
  };
}
