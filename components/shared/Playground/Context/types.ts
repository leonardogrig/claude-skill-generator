// Stub types for UI library - minimal types needed for hero input

export enum FormatType {
  Json = "json",
  Markdown = "markdown",
}

export enum Endpoint {
  Scrape = "scrape",
  Crawl = "crawl",
  Map = "map",
  Search = "search",
  Extract = "extract",
}
